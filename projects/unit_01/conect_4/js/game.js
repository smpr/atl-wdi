const playerOne = ();
const platerTwo = ();
 //ask play one and player 2 name
    //using input assign strings to playerOne and playerTwo and make it global
    //assign collors to eac player to be dropped into place
 //using turns have the playerOne's name display and say "alert(playerOne + " turn!") and the same for playerTwo
    //when the players turn comes they will select a collumn and it will drop down to the bottom of empty spots changing the color to the players collor then uses if else to check and see if they have 4 colors matching (try to figure out how to get it diagnoly)    
